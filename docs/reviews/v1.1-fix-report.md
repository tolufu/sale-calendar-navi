# v1.1 レビュー指摘 修正報告

作成日: 2026-05-29
担当: Codex（実装）
対象レビュー: `docs/reviews/v1.1-review.md`

## 対応内容

- 初期表示の参考リンク行 `{ label: "価格比較メモ", url: "" }` が保存時に `new URL("")` で失敗する問題を修正。
  - 参考リンク保存ロジックを `src/lib/utils/reference-link.ts` に純関数として切り出し。
  - URLが空の行はラベル有無に関わらず保存対象外。
  - URLあり・表示名なしは「参考リンクの表示名を入力してください。」を表示。
  - 不正URL・不正プロトコルは「参考リンクは http または https のURLで入力してください。」を表示。
  - 保存件数は5件まで。追加ボタンも5件で無効化。
- 欲しいもの登録・一覧編集の `desiredPrice` は、空欄または負値・不正数値を `null` として保存するよう修正。
- 一覧の外部リンク `rel` を `noopener noreferrer` に統一。
- テストを追加・補強。
  - 参考リンク検証: 空URL行スキップ、http/https許可、不正プロトコル拒否、表示名必須、5件上限。
  - 実質価格計算: 小数と `pointRate=0.5` の `Math.round`、0クランプ。
  - 楽天AF変換: 既存 `scid` を上書きしないこと。
  - データ移行: 負の希望価格と不正な参考リンクを正規化時に破棄すること。

## 追加対応（未対応事項）

- Playwrightを導入し、`npm run test:e2e` を追加。
  - PC（Chromium）とスマホ相当（Pixel 5）でフォーム保存フローを検証する設定を追加。
  - 参考リンク未入力の基本保存、比較候補と参考リンクを含む詳細保存のE2Eを追加。
- 登録フォームの詳細入力に比較候補UIを追加。
  - 主候補は既存の商品URL/EC/内訳入力を維持。
  - 任意の比較候補を追加し、EC、URL、商品価格、送料、クーポン値引き、付与ポイント、ポイント換算率、候補メモを保存可能にした。
  - 外部価格・画像の自動取得は行わない。
- 欲しいもの一覧のインライン編集で候補URLと内訳を編集可能にした。
- 欲しいもの一覧に並び替え、登録導線、セール状態バッジ、希望価格との関係バッジを追加。
  - 並び替え: 登録が新しい順 / 希望価格が低い順 / 関連セールが近い順。
  - 状態: 開催中 / 開催予定 / 終了 / セール未指定。
  - 希望価格表示: 希望価格内 / 希望価格までの差額。

## UI確認

- 参考リンク追加ボタンは5件到達時に無効化され、PC/スマホとも既存のグリッド・縦積み挙動を維持。
- 商品画像URL入力、スクレイピング、外部価格・画像取得、断定・保証表現は追加していない。

## 実行コマンド

- `pwd`
- `echo $WSL_DISTRO_NAME`
- `git status --short`
- `git branch --show-current`
- `node -v`
- `npm -v`
- `npm run test`
- `npm run lint`
- `npm run build`
- `npm run test:e2e`
- `npx playwright install chromium`
- `npx playwright install-deps chromium`
- `git diff --check`

## 確認結果

- `npm run test`: 8ファイル / 29件 PASS
- `npm run lint`: 警告・エラーなし
- `npm run build`: 成功
- `npm run test:e2e`: 失敗。PlaywrightのChromium起動時に `libnspr4.so` が見つからず停止。
  `npx playwright install-deps chromium` は sudo パスワードが必要で、この実行環境からは完了できなかった。
- `git diff --check`: 問題なし

## 未対応・次にやること

- WSL側でPlaywrightの依存ライブラリを導入後、`npm run test:e2e` を再実行する。
  例: `sudo npx playwright install-deps chromium`
