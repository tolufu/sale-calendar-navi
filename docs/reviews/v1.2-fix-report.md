# v1.2 レビュー指摘 修正報告

作業日: 2026-05-30
担当: Codex（実装）
対象: `docs/reviews/v1.2-review.md` の公開前必須修正 ①〜④

## 修正内容

- 価格比較シェア文面の末尾に `※手入力値・最新は各サイトで確認` を必ず含めるように修正。
- リマインダー生成で `item.merchantId` だけでなく `item.candidates[].merchantId` とセールの `merchantId` が一致する場合も通知候補に含めるように修正。
- 通知設定に `isValidNotificationEmail(email)` を追加し、メール入力時のみ形式エラーを表示して保存を抑止するように修正。
- プライバシーポリシーに Cookie・広告識別子、広告オプトアウト、データ削除手段の記載を追加。
- 追加対応として、履歴の全削除前に `window.confirm` で確認するように修正。

## 追加・更新したテスト

- `src/lib/utils/share.test.ts`
  - 価格比較シェア文面に手入力値・最新確認の注記が含まれることを検証。
- `src/lib/utils/reminders.test.ts`
  - `candidates` 経由で EC が一致する欲しいものが通知候補になることを検証。
- `src/lib/services/notifications.test.ts`
  - 通知メールアドレスの形式検証と保存前正規化を検証。

## 表示確認

dev サーバー（`http://127.0.0.1:3000`）を起動し、Playwright で次を確認。

- SEO: `/privacy` の title/canonical/OGP 出力、`/robots.txt`、`/sitemap.xml` の表示。
- 通知: `/settings/notifications` で不正なメール形式の保存時にエラーが表示されること。
- シェア: `/wishlist` の比較メモ共有で確認ダイアログが出ること、X intent URL に注記が含まれ、非公開メモが含まれないこと。
- 履歴: `/history` の履歴表示と全削除確認ダイアログ。
- 法的ページ: `/privacy` に Cookie・広告識別子、広告オプトアウト、データ削除の各セクションが表示されること。
- 広告枠: `/articles` に `data-ad-slot="articles-list-sidebar"` のプレースホルダーが表示されること。

## 実行コマンドと結果

- `pwd` / `echo $WSL_DISTRO_NAME` / `git status --short` / `git branch --show-current` / `node -v` / `npm -v`
  - Linux 側リポジトリ、`Ubuntu-24.04`、ブランチ `fix/v1-review`、Node `v22.22.3`、npm `10.9.8` を確認。
- `npm run lint`
  - 成功。ESLint 警告・エラーなし。
- `npm run test`
  - 成功。13 files / 39 tests passed。
- `npm run build`
  - 成功。17ページ生成完了。
- `npm run dev`
  - 表示確認用に起動。確認後に停止済み。
- Playwright 表示確認スクリプト
  - 成功。`display checks passed`。

## 禁止事項の確認

- 本番広告IDは追加していません。
- 実メール送信、SMTP、外部送信処理は追加していません。
- スクレイピング、外部ECの商品画像取得、価格自動取得は追加していません。
- 「最安保証」などの断定・保証表現は追加していません。
- Amazon/楽天等の公式サービスと誤認されるロゴ・表現は追加していません。

## 未対応・次にやること

- 記事カテゴリ/月別フィルタ、記事本文の構造化、実通知配信基盤は v2 バックログ扱い。
- 本番公開前に `NEXT_PUBLIC_SITE_URL` を本番URLに設定し、`sitemap.xml` と `robots.txt` のURLを確認する。
