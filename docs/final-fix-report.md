# 公開前ブロッカー修正レポート

- 対応日: 2026-05-30
- 対象ブランチ: `fix/v1-review`
- 対象監査: `docs/final-acceptance-audit.md`

## 修正結果

### 1. 追跡済みログ残骸の削除

- ルートの `tatus` を削除した。
- `.gitignore` への個別ルール追加は行っていない。

### 2. AC-012 公開コンテンツ整備

- 運営者情報を「セールカレンダー比較ナビ運営事務局」とし、連絡方法、サービス概要、収益化方針を記載した。
- お問い合わせページを `NEXT_PUBLIC_CONTACT_EMAIL` から生成する `mailto:` 導線へ変更した。
- `.env.example` と `README.md` に、公開時の `NEXT_PUBLIC_CONTACT_EMAIL` 設定を追加した。
- プライバシーポリシー、利用規約、フッターの説明を、楽天公式API画像の扱いと整合する文面へ更新した。
- 記事5本を、確認手順、条件メモ、購入前確認を含む実用的な内容へ加筆した。

### 3. AC-010 画像ポリシーのドキュメント整合

- `AGENTS.md`、`CLAUDE.md`、`docs/acceptance-checklist.md`、`README.md` を更新した。
- v1〜v1.2 は自作プレースホルダーのみ、v2以降は楽天公式APIが返した画像URLのみ出典明記で表示可能とした。
- ユーザー任意の画像URL入力、スクレイピング、無断取得・転載禁止のまま維持した。

## 最終確認

- `.env.example`: 本番キーや秘密情報なし。公開時に設定する連絡先メールの空欄を追加。
- `README.md`: 画像方針、秘密情報をコミットしない方針、公開時の連絡先設定を確認。
- 法的ページ: 公式サービスではない旨、価格等をスクレイピングで取得しない旨、広告・アフィリエイト表記、楽天公式API画像の扱いを確認。
- 広告枠: `Advertisement` と `審査前プレースホルダー` の表示を確認。
- 共有設定: `NEXT_PUBLIC_ENABLE_PUBLIC_SHARE` は空欄のままで、デフォルトONに変更していない。

## 確認コマンド結果

| コマンド | 結果 |
| --- | --- |
| `npm run lint` | PASS。警告・エラーなし。 |
| `npm run test` | PASS。20ファイル / 66テスト成功。 |
| `npm run build` | PASS。18ページ生成成功。 |

## 自己確認

- AC-010: PASS 相当。v2以降の画像ポリシーと実装の不整合を解消した。
- AC-012: PASS 相当。仮表記を除去し、公開用の運営者情報、問い合わせ導線、法的文面、記事本文を整備した。

## 未対応事項

以下は監査で公開後改善に分類されているため、今回変更していない。

- Firestore リポジトリの実装。
- 関連セールリンクのタイトル表示化。
- 記事詳細の固定共有バーとスマホ下部ナビの重なり解消。
- UIリッチ化、記事一覧のローディング・エラー状態追加。

公開環境では `NEXT_PUBLIC_SITE_URL` と `NEXT_PUBLIC_CONTACT_EMAIL` の実値を設定する。
