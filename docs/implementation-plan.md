# 実装計画 — セールカレンダー比較ナビ

最終更新: 2026-05-29

このドキュメントは Codex がそのまま着手できるフェーズ別タスクリスト。
要件は [requirements-summary.md](./requirements-summary.md)、構成は [architecture.md](./architecture.md)、
検収は [acceptance-checklist.md](./acceptance-checklist.md) を参照。

各タスクは可能な範囲で「Claudeで設計/レビュー → Codexで実装 → Claudeで確認」の順で進める。
完了の目安として対応する受け入れ基準（AC-xxx）を付記する。

---

## 共通: 着手前の前提タスク（v1の最初に一度だけ）

- [ ] **P-01** Next.js (App Router) + TypeScript + Tailwind CSS のプロジェクト初期化。
- [ ] **P-02** ディレクトリ構成・パスエイリアス・ESLint/Prettier・テスト基盤（Vitest + Testing Library）設定。
- [ ] **P-03** `.env.local.example` を用意（Firebaseキーは未設定でも起動できること）。秘密情報はコミットしない。
- [ ] **P-04** リポジトリ抽象化レイヤ（interface）と「Firestore実装／localStorage実装」の切替設計を用意。
- [ ] **P-05** `merchants` マスタの初期シード（amazon, rakuten）を用意。enumは使わない。
- [ ] **P-06** プレースホルダー画像を `public/images/placeholders/` に自作配置。外部画像は使わない。

---

## v1 MVP

**ゴール**: カレンダーで予定を見て、欲しいものをURL＋希望価格で登録できる。状態UIが共通化されている。

- [ ] **V1-01** ルーティングと共通レイアウト（ヘッダー/ナビ/フッター、レスポンシブ）。→ AC-020, AC-021
- [ ] **V1-02** 共通状態UIコンポーネント: `Skeleton` / `EmptyState` / `ErrorState` / `RetryButton` / `Toast`。
      v1から全画面で再利用する。 → AC-016〜AC-019
- [ ] **V1-03** `merchants` マスタの読み込みとEC色分けトークン適用。 → AC-002
- [ ] **V1-04** カレンダー画面: 月次グリッド、セールイベント表示、月送り。 → AC-001
- [ ] **V1-05** ECフィルター（merchantId単位の絞り込み、複数選択）。 → AC-002
- [ ] **V1-06** スマホの同日複数セール表示: 2件まで縦スタック / 3件以上は上位2件 + 「+n」バッジ /
      タップで当日一覧ボトムシート。 → AC-021
- [ ] **V1-07** セール詳細画面: イベント概要、紐づく欲しいものへの導線。 → AC-001
- [ ] **V1-08** 欲しいもの登録フォーム: 商品URL・希望価格・タイトル・merchant選択・対象セール紐づけ。
      URLバリデーション。 → AC-003, AC-004
- [ ] **V1-09** 欲しいもの一覧: カード表示（プレースホルダー画像）、編集・削除、空/読込/エラー状態。 → AC-005, AC-006
- [ ] **V1-10** Firebase Anonymous Auth 接続（未設定時はローカル匿名IDで代替）。 → AC-014
- [ ] **V1-11** localStorage フォールバックリポジトリ（Firebase未設定でも全CRUDが動く）。 → AC-015
- [ ] **V1-12** トップ画面: サービス説明・直近セール・主要導線。 → AC-001
- [ ] **V1-13** v1分の単体/コンポーネントテスト、`lint` / `test` / `build` 通過。

---

## v1.1 価格メモ・楽天アフィリエイト

**ゴール**: 実質価格メモと詳細入力折りたたみ、楽天URLのアフィリエイト変換。

- [ ] **V11-01** 実質価格メモ（`actualPriceMemo`）フィールドの追加と保存。ポイント還元等の自由記述。 → AC-007
- [ ] **V11-02** 登録フォームの詳細入力を**折りたたみ（アコーディオン）**化。基本項目は常時表示、
      詳細（メモ・対象セール等）は折りたたみ。 → AC-008
- [ ] **V11-03** 楽天商品URLの**アフィリエイトリンク変換**ユーティリティ。
      `merchants.affiliate` 設定（IDは環境変数）に基づき変換。規約範囲内のみ。 → AC-009
- [ ] **V11-04** 変換可否の判定（楽天以外/不正URLは非変換でそのまま）と単体テスト。 → AC-009
- [ ] **V11-05** 一覧・詳細での希望価格/実質価格メモ表示。「最安」等の断定表現を使わない文言で。 → AC-007
- [ ] **V11-06** `lint` / `test` / `build` 通過。

---

## v1.2 SEO・広告・通知・シェア・履歴

**ゴール**: 集客（SEO/OGP/シェア）、収益（広告枠）、継続（通知/履歴）。

- [ ] **V12-01** 記事機能（一覧/詳細, Markdown or MDX）。 → AC-012
- [ ] **V12-02** OGP/メタタグ（`generateMetadata`）。記事・主要ページにOG画像（自作）。 → AC-012
- [ ] **V12-03** 広告枠コンポーネント（所定位置に配置、公式誤認しない表記、未設定時はプレースホルダー）。 → AC-013
- [ ] **V12-04** 通知設定画面: ON/OFF・リードタイム（`leadDays`）・EC別設定（任意）保存。 → AC-010
- [ ] **V12-05** 通知トリガ設計（クライアント通知 or 後続でサーバ。v1.2はスケジュール設計と保存まで）。 → AC-010
- [ ] **V12-06** 購入履歴: 欲しいものから購入記録への変換、履歴画面で一覧・振り返り。 → AC-011
- [ ] **V12-07** シェア機能（Web Share API / URLコピー、シェア文に断定表現を使わない）。 → AC-012
- [ ] **V12-08** 復旧画面: エクスポート/インポート（JSON）でローカル⇔Firebase間のデータ移行を補助。 → AC-015
- [ ] **V12-09** `lint` / `test` / `build` 通過。

---

## v2 楽天API・買い時スコア初期版

**ゴール**: 公式API許諾範囲での正規データ取得と、買い時判断の初期スコア。

- [ ] **V2-01** 楽天公式API連携（規約・許諾の範囲で。スクレイピングはしない）。キーは環境変数。
- [ ] **V2-02** APIレート制限・キャッシュ・エラー時フォールバック設計。
- [ ] **V2-03** 取得データの正規化（merchantId/SaleEvent/価格参照）と保存。
- [ ] **V2-04** **買い時スコア初期版**: 希望価格・実質価格メモ・セール時期からの簡易スコア。
      「最安保証」ではなく「目安」と明示する。
- [ ] **V2-05** スコアのUI表示（カレンダー/一覧/詳細）と説明ツールチップ。
- [ ] **V2-06** `lint` / `test` / `build` 通過。

---

## v2.5+ EC追加・提携フィード準備

**ゴール**: merchant追加の運用と、提携フィード/正規データ連携の足場。

- [ ] **V25-01** 新規 merchant 追加フロー（マスタ追加だけで主要機能が動くことの確認）。
- [ ] **V25-02** merchant別アフィリエイト/フィード設定の抽象化拡張。
- [ ] **V25-03** 提携フィード（公式データ提供）の取り込みインターフェース設計（スクレイピング非依存）。
- [ ] **V25-04** EC追加時のテンプレ（色トークン・プレースホルダー・テスト）整備。
- [ ] **V25-05** スケール時のFirestoreインデックス/コスト見直し。

---

## 各フェーズ共通の完了条件

- 対象ACがチェックリストで満たされている。
- `npm run lint` / `npm run test` / `npm run build` が通る（失敗時は原因と対処を記録）。
- 禁止事項（スクレイピング/外部画像/断定表現/公式誤認/秘密情報コミット）に違反していない。
- Firebase未設定でもローカル（localStorage）で動作する。
